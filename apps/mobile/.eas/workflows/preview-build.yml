name: Create Preview Build
on:
  push:
    branches:
      - main
jobs:
  build_android:
    type: build
    params:
      platform: android
      profile: preview
      working_directory: apps/mobile
